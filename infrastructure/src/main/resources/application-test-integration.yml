spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: root
    password: 123456
    url: jdbc:h2:mem:adm_videos_test;MODE=MYSQL;DATABASE_TO_LOWER=TRUE; # É possivel persistir em um alternando "mem:adm_videos_test" pora "file:./.h2/dev"

  jpa:
#    defer-datasource-initialization: true
    hibernate:
      ddl-auto: 'create-drop'
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2


