# DataSource Config
#spring.datasource.url=jdbc:mysql://localhost:3306/admin_videos
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/admin_videos
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root

  # RabbitMQ Config
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Keycloak Config
keycloak:
  auth-server-url: http://localhost:8080/auth